---
---

<section id="state-of-enough" class="enough-section">
  <div class="enough-container">
    <blockquote class="enough-statement">
      "A study in containment. We design for the quiet space between excess and necessity—carrying only what holds meaning."
    </blockquote>
  </div>
</section>

<style>
  .enough-section {
    padding: var(--spacing-xl) 0;
    background-color: var(--color-bg);
    display: flex;
    justify-content: center;
    align-items: center;
    text-align: center;
  }

  .enough-container {
    max-width: 800px;
    padding: 0 var(--spacing-md);
  }

  .enough-statement {
    font-family: var(--font-serif);
    font-size: var(--text-lg);
    line-height: 1.8;
    color: var(--color-text-primary);
    letter-spacing: 0.02em;
    font-style: italic;
  }

  @media (max-width: 767px) {
    .enough-section {
      padding: var(--spacing-lg) 0;
    }
    
    .enough-statement {
      font-size: var(--text-base);
      line-height: 1.6;
    }
  }
</style>
